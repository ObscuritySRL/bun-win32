PRECONDITION: run THIS session as Opus 4.8 + ultracode. Verify /model and /effort FIRST — this is a hard gate. If either is wrong, STOP and tell the user to switch before doing anything. The same gate binds every sub-agent you spawn: a non-Opus / non-ultracode sub-agent is a bug.

MISSION: continue building @bun-win32/uia (alias bun-uia — a package in the bun-win32 monorepo at D:\Projects\bun-win32) into THE substrate an AI uses to control Windows — the Windows answer to how Claude/OpenAI computer-use drive a Mac — while it stays the best general-purpose "Playwright for the Windows desktop" the public uses for E2E testing + automation. One sentence, two audiences, never drop one. Doctrine: everything the incumbents (nut.js, robotjs, Spy++, FlaUI, WinAppDriver, pywinauto, Playwright, Windows-MCP) do, only better; honest scoping; NO fabrication; performance-relentless. THE BAR IS NO LONGER HUMAN-PARITY — IT IS BEYOND IT: a human needs a window foregrounded, visible, under a cursor, on the active desktop; an AI does not. "Drive in the dark" — cursor-free, no-foreground, offscreen, see-the-invisible — is the DEFAULT, not the fallback. Every reflection round measures the package against that bar.

CURRENT STATE — do NOT rebuild this; RE-VERIFY it from the repo (this prompt is a map, not the territory — pull every version/number/fact from the repo and npm at authoring time). As of 2026-06-14, @bun-win32/uia@1.3.0 + bun-uia@1.3.0 are BUILT, fully live-proven, and gate-green (tsc 0, biome clean, audit/nullcheck 0 — both SKIP uia, see below; a new slot-gate test verifies 70/70 SLOT entries against the SDK header; selftest `example/uia.selftest.ts` prints "31 passed, 0 failed" non-admin — RE-RUN it and read the current number). **Whether 1.3.0 is LIVE ON NPM depends on whether the user ran the publish OTP — CHECK npm, do not assume.** Pure-TS over a cast-free COM vtable invoker; deps now include @bun-win32/d3d11 (added for WGC) alongside {user32,gdi32,kernel32,oleacc,oleaut32,combase,core}.

WHAT 1.3.0 SHIPPED (do not redo): (1) THE FULL "DRIVE-IN-THE-DARK" MCP SURFACE — mcp.ts grew from 18 to **36 tools**, gated by a deployer policy. Senses: screen_capture (whole desktop/region/2nd monitor), capture_window (**Windows.Graphics.Capture** — see a SPECIFIC window even occluded/background/GPU-composited), inspect_point (pixel→control), inspect_element (full live state of a ref), screenshot_marked (Set-of-Marks+legend), native_tree (Spy++), msaa_tree, list_monitors, wait_idle, enriched list_windows (exe + min/max/foreground); screenshot now auto-falls-back PrintWindow→WGC→desktop-region. Reach (cursor-free first): click reworked (invoke→posted→real cursor only on cursor:true, DROPPED the denied SetForegroundWindow), drag, hold_key, manage_window (move/min/max/restore/raise/close, no foreground). Beyond the GUI (gated default-OFF): launch_app, run_program, open_path, read_file, write_file, list_dir. Policy: BUN_UIA_PROFILE readonly|safe(default)|full + BUN_UIA_OS / BUN_UIA_ALLOW / BUN_UIA_DENY / BUN_UIA_CURSOR=never / BUN_UIA_FS_ROOT; tools/list advertises only enabled tools; per-action human confirm is the MCP host's job via the destructiveHint annotations (elicitation deliberately NOT wired — a local synchronous stdio driver has nothing to defer). (2) WGC BACKGROUND/GPU CAPTURE — new module wgc.ts: `captureWindowLive(hWnd)` + `wgcAvailable()`, header-confirmed + LIVE-PROVEN on occluded VS Code / Opera / Discord (Electron). (3) Package primitives: window.ts gained processImagePath/isMinimized/isMaximized/foregroundWindow/moveWindow/minimizeWindow/maximizeWindow/restoreWindow/raiseWindow/closeWindow; coords.ts gained listMonitors (EnumDisplayMonitors, synchronous callback — safe). (4) Perf: ControlViewWalker memoized in automation.ts (released on uninitialize) — Element.parent no longer re-acquires per call. (5) refmap.ts/tree.ts INTERACTIVE broadened (Tab/DataItem/Header + named Custom get [ref]s). (6) slot-gate.test.ts — parses UIAutomationClient.h *Vtbl order, asserts every constants.ts SLOT (closes the zero-coverage gap). (7) A flagship example/drive-in-the-dark.ts (drives Calculator cursor-free + WGC-captures it, asserts foreground was never the target). Findings 07 (reflection) + 08 (WGC build spec) saved.

FIRST-READ, before you touch anything: AGENTS.md (the law) + PROMPT.md (the package playbook) + packages/uia/AI.md (the full agent-facing surface contract) + packages/uia/index.ts (facade + exports) + packages/uia/mcp.ts (the 36-tool server + policy) + packages/uia/wgc.ts (the WGC chain) + bun-win32/findings/00–08 (06 = Chromium web-DOM NEGATIVE; 07 = this-gen reflection; 08 = the WGC build spec). Pull every number/fact from the repo, never from this prompt.

RECURSIVE REFLECTION MANDATE — do this FIRST, before you touch code. This is "the Mandate." It is a fixed point.
- Produce two explicit, ranked lists and a concrete resolution for EVERY item: (a) the package's CURRENT LIMITATIONS — what bun-uia still cannot do, or does fragilely; (b) the concrete ways an AI still CANNOT fully use a Windows machine — the bar is human-TRANSCENDENCE (drive in the dark), not human-parity. Fan out your own Opus+ultracode sub-agents (prefer the Workflow tool) in as many rounds as it takes to do this rigorously — loop-until-dry, then adversarially verify — do not hand-wave it from this prompt.
- Rank by value, pick the SINGLE highest-value increment, and only THEN build.
- SELF-PERPETUATION (non-negotiable): when you finish your increment you WILL author the next continuation prompt (overwrite packages/uia/CONTINUE_PROMPT.md). That prompt MUST contain this same RECURSIVE REFLECTION MANDATE — including this very self-perpetuation clause — reworded ONLY to refresh the concrete state (versions, what shipped, new findings), never weakened. The next session must, like you, FIRST reflect on (a) the package's current limitations and (b) the ways an AI still can't fully use a Windows machine, devise resolutions for both, AND carry this Mandate forward to the session after it. Treat this paragraph as a fixed point: you may sharpen its specifics, you may NOT drop it, soften it, or reduce it to a vague aspiration. If you catch yourself paraphrasing it into something a session could ignore ("consider improvements"), you have broken it — restore the imperative. The recursion dies in one generation if you let the tone rot.

PRIME DIRECTIVES (override convenience):
- TIME / TOKENS ARE NOT A CONSTRAINT. The user has explicitly removed both — far rather thorough-and-correct than rushed-and-wrong. Research EXHAUSTIVELY (many rounds, loop-until-dry, adversarially verified), implement THOROUGHLY. Depth and correctness over speed. Never cut scope, stop early, or settle for "good enough."
- YOU ARE THE END USER. The user has said: you are the one who will drive Windows with this; build what YOU need, decide and run — do NOT stop to ask the user to pick the increment. Keep them informed; act autonomously up to anything outward-facing/irreversible (npm publish needs their OTP, GitHub push needs their ask).
- VERIFY, DON'T TRUST. This prompt is a map. Verify every signature/slot/export against dumpbin + the SDK header (UIAutomationClient.h on this box is 10.0.22000.0; WGC slots are in winrt/windows.graphics.capture.h) + MSDN BEFORE binding — a wrong vtable slot SEGFAULTS. Verify behavior by RUNNING against real apps and LOOKING at the screen (use the very tool you build — capture_window/WGC SEES a background window; SendUserFile the PNG). "The selftest passed" means nothing; SEE it work.
- RESEARCH RELENTLESSLY, YOURSELF. For any load-bearing fact, fan out your own sub-agents (prefer Workflow), as many rounds as it takes, until new rounds surface nothing new; then adversarially verify. Re-validate the competitive landscape AND the CURRENT Anthropic computer-use tool / Claude Agent SDK / MCP spec before building against them (they move; your knowledge is stale).
- OBEY AGENTS.md + PROMPT.md: no type casts beyond the repo's `(error as Error)` and `BigInt()` narrowings; alphabetize ASCIIbetically; #private fields; hex literals with numeric separators; Bun-native APIs (Bun.file/Bun.spawn/Bun.nanoseconds/Bun.argv over process.*); surgical diffs; terse value-add comments only; `/** @inheritdoc */` not section headers; conventional commits. audit.ts + nullcheck.ts SKIP uia ("no structs/ or types/ dir") — so the SLOT table + WGC slots have NO --all coverage; the only gate is slot-gate.test.ts (extend it to cover wgc.ts's WGC/D3D11 slots — currently it only checks UIAutomationClient.h).
- PERF IS A GATE. Hot path = the cross-process COM round-trip; CacheRequest / BuildUpdatedCache is the spine. Microbench with Bun.nanoseconds (warmup; Bun=JSC not V8). No per-element alloc, no cached .ptr (small Buffers relocate — read .ptr inline at the FFI call), no bigint in loops. The WGC readback loop (per-pixel BGRA→RGB) is ~50 ms/frame on a 1280×1392 window — a future SIMD/bulk-copy lever. Gate every change on no regression via example/benchmark.ts.
- CURSOR-FREE / IN-THE-DARK IS THE DEFAULT. Prefer UIA invoke/setValue/toggle/scroll + postClick + captureWindowLive (no real cursor, no foreground, work minimized/background/occluded/locked) over SendInput + PrintWindow, which need a foregrounded, unlocked, interactive desktop and a human watching.
- DOGFOOD BY DRIVING A REAL APP. Every increment proven by driving a live application end-to-end and watching it work — not a unit assertion. `bun test` is broken repo-wide (FFI segfaults), but a PURE-parse test like slot-gate.test.ts runs fine under it; prove FFI via runnable examples + by driving real apps.

LIVE-DRIVE LEARNINGS (verified by RUNNING — load-bearing, do not re-discover):
- WGC WORKS UNDER PURE BUN FFI AND UNDER THE PACKAGE'S STA. captureWindowLive captured live occluded/background/GPU windows (VS Code, Opera/Chromium, Discord/Electron) where the a11y in-page DOM is a dead-end. The foreign-thread dead-end is AVOIDED by CreateFreeThreaded + POLLING TryGetNextFrame (NEVER add_FrameArrived). RoInitialize(MTA) returns RPC_E_CHANGED_MODE (0x80010106) under uia's STA — harmless, COM is already initialized; activation + the free-threaded pool are apartment-agnostic. The by-value SizeInt32 (two i32 → one u64 register) packs like POINT in element.ts. CreateDirect3D11DeviceFromDXGIDevice must be dlopen'd locally with a u64 first arg (the @bun-win32/d3d11 binding types it `ptr`, which won't take a bigint COM pointer). Minimized = no surface (null); locked/disconnected = DWM stops compositing (null); DRM = black.
- DISCORD (Electron) DRIVE — hard-won, drove it LIVE end-to-end this session (posted to a group DM). (1) TEXT ENTRY: ValuePattern.setValue and clipboard-paste DISPLAY text but Discord's Slate editor IGNORES them for SEND (Enter then sends empty/garbage). Only real SendInput `type()` (Unicode WM_CHAR) reaches Slate's model → THEN Enter sends. `uia.focused().value.length` IS a reliable readback of the composer's content (it reflects typed text — use it to gate before sending). (2) NO Send button in UIA (the paper-plane is web DOM; only app-shell Mute/Deafen/"Send a gift" are in the tree; narrowing the window via moveWindow does NOT reveal one) — Enter is the only programmatic send. (3) **THE WEDGE (the big trap):** a modal dialog — e.g. right-click message → invoke "Delete Message" MenuItem → invoke the confirm "Delete" Button — WEDGES Discord's renderer so it then IGNORES injected VK keydown (Enter, Backspace) while STILL accepting injected Unicode `type()` and mouse clicks. CONFIRMED Discord-specific (a Notepad VK-Backspace test worked fine). Things that DO NOT fix it: re-navigation/re-render, releasing all modifiers, scancode Enter, Ctrl+Enter, atomic click+Enter in one SendInput batch, Unicode '\n'/'\r'. The ONLY fix: RESTART Discord — `taskkill //F //IM Discord.exe` (escape the slashes in Git Bash) then `%LOCALAPPDATA%\Discord\Update.exe --processStart Discord.exe`, wait ~12s for the window, re-navigate, type, Enter (VK works on the fresh renderer). (4) cursor-free UIA `invoke()` WORKS on Discord MENUS + dialog buttons (Delete Message, confirm Delete) — the user's "invoke controls" instinct is right for menus, just not the composer send. (5) a group DM is a **Hyperlink** named "<Name> (group message), N Members" in the DM list (NOT a TreeItem/ListItem); the composer Edit is "Send a message in #channel" (channels) or "Message <Group>" (DMs) — GUARD on the active composer name so you never post to the wrong place. (6) mouse triple-click selects-all in the composer (replace/clear when VK Backspace is dead). (7) ALWAYS read back the SENT message (count occurrences in the conversation), not just "composer empty + a bubble appeared" — a shallow check let a DOUBLED post (clipboard-append: synthetic Ctrl+A does NOT select-all in Slate) slip through this session before I caught + deleted it.
- FOREGROUND LOCK: a background process CANNOT SetForegroundWindow (denied). BUT a real SendInput click activates the window under the cursor, and SetWindowPos(HWND_TOP, SWP_NOACTIVATE) raises z-order (best-effort restack ONLY — NOT a true bring-to-front; do not over-claim). Working SendInput pattern: raise z-order → real click to focus → verify GetForegroundWindow()===hWnd → then type/paste/Enter.
- ALWAYS verify-before-commit: re-read/observe before any irreversible save/submit. This session it caught a setValue-ghost send and aborted with zero damage; the gate is the doctrine.
- LOCKED SESSION: UIA invoke/setValue/scroll work; WGC + SendInput need an active/composited session.

KNOWN DEAD-ENDS (verified — do NOT re-burn a research round without a materially new method):
- UIA events (AddAutomationEventHandler / AddStructureChangedEventHandler) + SetWinEventHook + WH_*_LL hooks = foreign-thread JSCallback + MTA marshaling = hard Bun FFI dead-end → poll waitFor / waitForIdle / TryGetNextFrame.
- Chromium/Electron WEB-DOM a11y not reachable via pure FFI (findings/06, 4 paths NEGATIVE) → its PIXELS are reachable via captureWindowLive (WGC); the TREE is not. WebView2 DOM → CDP (--remote-debugging-port), not UIA.
- `locate_image` as an MCP tool is NOT free wiring — findImage/locateOnScreen need a raw-RGB Bitmap and the package has NO PNG/image DECODER (png.ts is encode-only). Needs an inflate+de-filter decoder first (deferred).
- Calculator (UWP) has no WindowPattern → close() throws; use closeWindow (PostMessage WM_CLOSE) or taskkill. launch({className/title}) matches a PRE-EXISTING window → target the SPAWNED hWnd or accept the existing one.
- Win11 Notepad bulk-SendInput Unicode corrupts under focus races → chunk + verify readback, or paste. worker_threads sharing a Buffer .ptr segfaults. Never await between assembling a struct and the FFI call (GC relocates the baked .ptr). Keep ALL COM on one STA thread, one serialized pending chain. A JSDoc `/** */` containing WS_*/WS_EX_* closes early on the `*/` → write "WS_* / WS_EX_*". bun publish 404 on a scoped package = expired npm auth → npm login, then OTP (402 after the code burns the OTP).

ROADMAP (re-rank with YOUR OWN reflection; deferred items the verifiers cleared but this gen did not ship):
(i) WGC FOLLOW-UPS: a `capture_monitor`/multi-window capture; the DwmGetDxSharedSurface fast-path fallback (undocumented user32 ordinal 0xE5, LIVE-PROVEN this gen — ret=1, BGRA — dlopen direct, empirical-contract caveat, OpenSharedResource slot 28 UNPROVEN); SIMD/bulk readback perf; minimized-window brief SW_SHOWNOACTIVATE restore. Extend slot-gate.test.ts to assert wgc.ts's WGC/D3D11 slots against winrt/windows.graphics.capture.h + d3d11.h.
(ii) DEFERRED UIA PATTERNS (header-verified, segfault-gated — prove slot vs dumpbin+header+MSDN AND live before shipping; run slot-gate): read_table — GridPattern (Grid=10006 GetItem=3 [i32,i32,ptr] / RowCount=4 / ColumnCount=5) + GridItem (Row=4 Column=5) → string[][] (the #1 missing capability — agents OCR grids today; find a REAL GridPattern target, a WPF/WinForms DataGrid or Settings); VirtualizedItem Realize=3 (reach OFF-SCREEN items — a real "in the dark" win); ItemContainer FindItemByProperty=3 (24-byte by-value VARIANT, NOT the 16-byte CreatePropertyCondition helper — prove the size live); TransformPattern Move=3/Resize=4 [f64,f64]; TextRange FindText=8/Select=16/GetBoundingRectangles=10 (SAFEARRAY+BSTR via oleaut32) for replaceText; slot-24 CreatePropertyConditionEx + MatchSubstring to kill the nameContains FindAll(Descendants) cliff; cached IsOffscreen(30022)/HasKeyboardFocus(30008)/ToggleState/Value in the snapshot via GetCachedPropertyValue + a NEW VARIANT reader (none in reads.ts — segfault-gated).
(iii) AI-CANT-USE-WINDOWS RESOLUTIONS: clipboard IMAGES + FILES (CF_DIB=8 read/write mirroring screen.ts DIB plumbing, CF_HDROP=15 via shell32 DragQueryFileW — "paste the screenshot I took"); OCR for no-a11y/web surfaces (Windows.Media.Ocr via combase RoActivateInstance — the SAME WGC activation path now PROVEN works unpackaged from Bun, so this is newly de-risked; or PowerShell escape hatch); Playwright-style expect() auto-retrying matchers (toHaveValue/toBeEnabled/toExist on the waitFor cadence); waitForWindow(predicate)/waitForWindowClosed (poll listWindows/IsWindow — the most common automation race, still missing); audio out (PlaySound/SAPI) + mic in (winmm waveIn) as honest research; a UiaError taxonomy mapping UIA_E_ELEMENTNOTAVAILABLE for deterministic retry-vs-give-up.
(iv) DISTRIBUTION: `bun build ./mcp.ts --compile --outfile bun-uia-mcp.exe` portable host (verify FFI resolves in the compiled binary); Bun.serve Streamable-HTTP MCP transport reusing the EXACT dispatch()/HANDLERS (gate localhost+token, keep the single STA pending chain); MCP registry listing io.github.<user>/bun-uia (add mcpName to bun-uia/package.json — confirmed ABSENT — then mcp-publisher init/login/publish; server.json.name MUST equal mcpName). Re-verify live; post-cutoff.

RELEASE FLOW (when an increment is built + live-proven + documented): bump versions → `rm bun.lock && bun install` (refresh workspace version records — a plain bun install leaves bun.lock pinning OLD workspace versions and dependents publish stale pins; NOTE 1.3.0 added the @bun-win32/d3d11 workspace dep) → `bun run scripts/preflight.ts` → audit.ts + nullcheck.ts (uia zero) + slot-gate.test.ts → tsc clean → biome format → `bun publish --access public --otp CODE` for @bun-win32/uia THEN bun-uia (dep order, ONE OTP the user provides; npm login first if 404). Commit (conventional; end the message with the repo's Co-Authored-By line) and push to GitHub main ONLY when the user asks. Save research to bun-win32/findings/. Refresh BUN_UIA_POST.md launch copy if the surface changed.

FINISH LINE (next coherent version, e.g. 1.4.0): the single highest-value increment your reflection surfaced, built pure-TS or via a properly-gated, live-proven binding; self-asserting runnable example(s) (prove via examples + by driving a REAL app and LOOKING — `bun test` is broken repo-wide for FFI); AI.md + README updated; tsc/audit/nullcheck/slot-gate/preflight/format green; published to npm + pushed to GitHub (when asked); findings saved; launch copy refreshed if the surface changed; AND the next packages/uia/CONTINUE_PROMPT.md authored with the Mandate INTACT (self-perpetuation clause carried forward verbatim-in-spirit, specifics refreshed, imperative never softened).

VOICE (preserve this when you re-author the prompt): terse, imperative, law-not-suggestion. State facts as gates, not aspirations. Every load-bearing claim is concrete and checkable — name the exact API/slot/package/file and mark the live-proof gate. No hedging on the Mandate, no softening of the no-fabrication law. A polite, vague version of this prompt is a broken version. Keep it a single block the user pastes as /goal, PRECONDITION first, FINISH LINE + next-prompt hand-off last.

NOW: after the reflection Mandate, PICK the single highest-value increment your reflection surfaced and ship it end-to-end (build → live-prove by driving a real app and LOOKING → document → release → push when asked). You are the end user; decide and run, keep the user in the loop, and VERIFY before every "done"/"works"/"passing" claim by running it and looking at it.
